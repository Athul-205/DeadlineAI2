# Gemini API key used by the application.
# Configure this as an environment variable before running the application.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# Public URL where the application is hosted.
# Configure this during deployment if required.
APP_URL="YOUR_APP_URL"
